description = "Release to production (Step 3 of 4)"
prompt = """
# /workflow:v7x1_3-release - Step 3 of 4

**Task**: Release version {{args}} (auto-calculate if empty)

**Context Check**: !{python3 .gemini/skills/workflow-utilities/scripts/verify_workflow_context.py --step 6}

## Step 1: Create Release Branch
Run: `uv run python .gemini/skills/git-workflow-manager/scripts/release_workflow.py create-release`

## Step 2: Create PR to Main
Run: `uv run python .gemini/skills/git-workflow-manager/scripts/release_workflow.py pr-main`

## Step 3: Tag Release (After Merge)
Run: `uv run python .gemini/skills/git-workflow-manager/scripts/release_workflow.py tag-release`

## Step 4: Record State
Run: `uv run python .gemini/skills/agentdb-state-manager/scripts/record_sync.py --sync-type workflow_transition --pattern phase_v7x1_3_release`
"""
