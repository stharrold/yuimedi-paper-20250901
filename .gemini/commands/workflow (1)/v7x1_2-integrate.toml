description = "Integrate feature to develop (Step 2 of 4)"
prompt = """
# /workflow:v7x1_2-integrate - Step 2 of 4

**Task**: Integrate feature branch: {{args}}

**Mode Detection**:
- If {{args}} is empty: Contrib-only mode (direct PR contrib -> develop)
- If {{args}} has branch: Full mode (feature -> contrib PR, cleanup, contrib -> develop PR)

## Step 1: feature -> contrib [FULL MODE]
Push {{args}} and create PR to `contrib/stharrold` using `gh pr create`.

## Step 2: Cleanup [FULL MODE]
Run: `uv run python .gemini/skills/git-workflow-manager/scripts/cleanup_feature.py {slug}`

## Step 3: contrib -> develop
Push `contrib/stharrold` and create PR to `develop`.

## Step 4: Record State
Run: `uv run python .gemini/skills/agentdb-state-manager/scripts/record_sync.py --sync-type workflow_transition --pattern phase_v7x1_2_integrate`
"""
